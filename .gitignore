.vscode
**/*.env
**/*.credentials
**/kubeconfig
# Cluster admin credentials fetched by `make get-kubeconfig`. chmod 600 protects
# them on disk; this keeps them out of a `git add -A`.
.k3s.yaml
.k3s.yaml.bak
**/*-secret.yaml
**/tmp/

# Session handoff scratchpad. Deliberately untracked -- it is working state, not
# a versioned artifact, and it was generating a commit per session. Durable
# findings belong in docs/ or CLAUDE.md, which ARE tracked.
# Last tracked version: `git show dc7e4e0:STATE.md`
STATE.md

# macOS finder metadata
.DS_Store
**/.DS_Store
